# Requires Python 3.11
fastapi
uvicorn
python-multipart
unstructured[local-inference]
python-dotenv
aiofiles
beautifulsoup4
# pdfplumber can be removed if no longer used
# pypandoc and python-docx can be temporarily commented out or removed if we simplify export for now 